name: drop_down_testapp
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  rashail_multi_dropdown: ^0.0.1+5
  ninja_dropdown: ^0.1.0+3
  multiselect_nested: ^1.1.0
  multiselect: ^0.1.0
  multiple_search_selection: ^2.5.4
  multi_dropdown: ^2.1.0
  drop_down_list: ^0.0.5
  animated_custom_dropdown: ^2.0.0
  dropdown_button2: ^2.3.9
  dropdown_search: ^5.0.6

  dio: ^5.3.3

  supabase_flutter: ^1.10.25
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/example_data.json
    - .env
